// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file dsf_define_messagesPubSubTypes.cpp
 * This header file contains the implementation of the serialization functions.
 *
 * This file was generated by the tool fastddsgen.
 */

#include "txdds/RTPS/common/SerializedPayload.h"

#include "txcdr/Cdr.h"
#include "txcdr/CdrSizeCalculator.h"
#include "txcdr/SerializeCdr.h"
#include "txcdr/DeserializeCdr.h"

#include "dsf_define_messagesPubSubTypes.h"
#include "dsf_define_messagesCdrAux.hpp"

using SerializedPayload = BaoSky::rtps::SerializedPayload;
using InstanceHandle = BaoSky::rtps::InstanceHandle;

namespace DSF {
    namespace Message {
        EventPubSubType::EventPubSubType()
        {
            SetName("DSF::Message::Event");
            uint32_t type_size = DSF_Message_Event_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        EventPubSubType::~EventPubSubType()
        {
        }

        bool EventPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            Event *p_type = static_cast<Event *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool EventPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            Event *p_type = static_cast<Event *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> EventPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<Event *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* EventPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new Event());
        }

        void EventPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<Event *>(data));
        }

        bool EventPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        EventTransferPubSubType::EventTransferPubSubType()
        {
            SetName("DSF::Message::EventTransfer");
            uint32_t type_size = DSF_Message_EventTransfer_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        EventTransferPubSubType::~EventTransferPubSubType()
        {
        }

        bool EventTransferPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            EventTransfer *p_type = static_cast<EventTransfer *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool EventTransferPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            EventTransfer *p_type = static_cast<EventTransfer *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> EventTransferPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<EventTransfer *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* EventTransferPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new EventTransfer());
        }

        void EventTransferPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<EventTransfer *>(data));
        }

        bool EventTransferPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        NamedBlobPubSubType::NamedBlobPubSubType()
        {
            SetName("DSF::Message::NamedBlob");
            uint32_t type_size = DSF_Message_NamedBlob_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        NamedBlobPubSubType::~NamedBlobPubSubType()
        {
        }

        bool NamedBlobPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            NamedBlob *p_type = static_cast<NamedBlob *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool NamedBlobPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            NamedBlob *p_type = static_cast<NamedBlob *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> NamedBlobPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<NamedBlob *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* NamedBlobPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new NamedBlob());
        }

        void NamedBlobPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<NamedBlob *>(data));
        }

        bool NamedBlobPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        ControlVarPubSubType::ControlVarPubSubType()
        {
            SetName("DSF::Message::ControlVar");
            uint32_t type_size = DSF_Message_ControlVar_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        ControlVarPubSubType::~ControlVarPubSubType()
        {
        }

        bool ControlVarPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            ControlVar *p_type = static_cast<ControlVar *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool ControlVarPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            ControlVar *p_type = static_cast<ControlVar *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> ControlVarPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<ControlVar *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* ControlVarPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new ControlVar());
        }

        void ControlVarPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<ControlVar *>(data));
        }

        bool ControlVarPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        ControlPubSubType::ControlPubSubType()
        {
            SetName("DSF::Message::Control");
            uint32_t type_size = DSF_Message_Control_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        ControlPubSubType::~ControlPubSubType()
        {
        }

        bool ControlPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            Control *p_type = static_cast<Control *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool ControlPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            Control *p_type = static_cast<Control *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> ControlPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<Control *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* ControlPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new Control());
        }

        void ControlPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<Control *>(data));
        }

        bool ControlPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        SubTableRegisterPubSubType::SubTableRegisterPubSubType()
        {
            SetName("DSF::Message::SubTableRegister");
            uint32_t type_size = DSF_Message_SubTableRegister_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        SubTableRegisterPubSubType::~SubTableRegisterPubSubType()
        {
        }

        bool SubTableRegisterPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            SubTableRegister *p_type = static_cast<SubTableRegister *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool SubTableRegisterPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            SubTableRegister *p_type = static_cast<SubTableRegister *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> SubTableRegisterPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<SubTableRegister *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* SubTableRegisterPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new SubTableRegister());
        }

        void SubTableRegisterPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<SubTableRegister *>(data));
        }

        bool SubTableRegisterPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        SubTableUnregisterPubSubType::SubTableUnregisterPubSubType()
        {
            SetName("DSF::Message::SubTableUnregister");
            uint32_t type_size = DSF_Message_SubTableUnregister_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        SubTableUnregisterPubSubType::~SubTableUnregisterPubSubType()
        {
        }

        bool SubTableUnregisterPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            SubTableUnregister *p_type = static_cast<SubTableUnregister *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool SubTableUnregisterPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            SubTableUnregister *p_type = static_cast<SubTableUnregister *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> SubTableUnregisterPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<SubTableUnregister *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* SubTableUnregisterPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new SubTableUnregister());
        }

        void SubTableUnregisterPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<SubTableUnregister *>(data));
        }

        bool SubTableUnregisterPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        AlarmPubSubType::AlarmPubSubType()
        {
            SetName("DSF::Message::Alarm");
            uint32_t type_size = DSF_Message_Alarm_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        AlarmPubSubType::~AlarmPubSubType()
        {
        }

        bool AlarmPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            Alarm *p_type = static_cast<Alarm *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool AlarmPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            Alarm *p_type = static_cast<Alarm *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> AlarmPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<Alarm *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* AlarmPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new Alarm());
        }

        void AlarmPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<Alarm *>(data));
        }

        bool AlarmPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        NamedRetcodePubSubType::NamedRetcodePubSubType()
        {
            SetName("DSF::Message::NamedRetcode");
            uint32_t type_size = DSF_Message_NamedRetcode_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        NamedRetcodePubSubType::~NamedRetcodePubSubType()
        {
        }

        bool NamedRetcodePubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            NamedRetcode *p_type = static_cast<NamedRetcode *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool NamedRetcodePubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            NamedRetcode *p_type = static_cast<NamedRetcode *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> NamedRetcodePubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<NamedRetcode *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* NamedRetcodePubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new NamedRetcode());
        }

        void NamedRetcodePubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<NamedRetcode *>(data));
        }

        bool NamedRetcodePubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }

        CommandResponsePubSubType::CommandResponsePubSubType()
        {
            SetName("DSF::Message::CommandResponse");
            uint32_t type_size = DSF_Message_CommandResponse_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

        }

        CommandResponsePubSubType::~CommandResponsePubSubType()
        {
        }

        bool CommandResponsePubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            CommandResponse *p_type = static_cast<CommandResponse *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return true;
        }

        bool CommandResponsePubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            CommandResponse *p_type = static_cast<CommandResponse *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            deser.Deserialize(*p_type);

            return true;
        }

        std::function<uint32_t()> CommandResponsePubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<CommandResponse *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* CommandResponsePubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new CommandResponse());
        }

        void CommandResponsePubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<CommandResponse *>(data));
        }

        bool CommandResponsePubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            // TODO key relate
            return true;
        }


    } //End of namespace Message


} //End of namespace DSF

