// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file dsf_define_datatypesPubSubTypes.cpp
 * This header file contains the implementation of the serialization functions.
 *
 * This file was generated by the tool fastddsgen.
 */

#include "txdds/RTPS/common/SerializedPayload.h"

#include "txcdr/Cdr.h"
#include "txcdr/CdrSizeCalculator.h"
#include "txcdr/SerializeCdr.h"
#include "txcdr/DeserializeCdr.h"

#include "dsf_define_datatypesPubSubTypes.h"
#include "dsf_define_datatypesCdrAux.hpp"

using SerializedPayload = BaoSky::rtps::SerializedPayload;
using InstanceHandle = BaoSky::rtps::InstanceHandle;

namespace DSF {
    TimespecPubSubType::TimespecPubSubType()
    {
        SetName("DSF::Timespec");
        uint32_t type_size = DSF_Timespec_max_cdr_typesize;

        type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
        mTypeSize = type_size + 4;

        mIsGetKeyDefined = false;

        uint32_t keyLength = DSF_Timespec_max_key_cdr_typesize > 16 ? DSF_Timespec_max_key_cdr_typesize : 16;
        mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
        memset(mKeyBuffer, 0, keyLength);
    }

    TimespecPubSubType::~TimespecPubSubType()
    {
        if (mKeyBuffer != nullptr)
        {
            free(mKeyBuffer);
        }
    }

    bool TimespecPubSubType::Serialize(
        void *data,
        SerializedPayload *payload)
    {
        Timespec *p_type = static_cast<Timespec *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Serialize encapsulation
        ser.SerializeEncapsulation();
        // Serialize the object.
        auto retcode = ser.Serialize(*p_type);

        // Get the serialized length
        payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
        return retcode == 0;
    }

    bool TimespecPubSubType::Deserialize(
        SerializedPayload* payload,
        void* data)
    {
        // Convert DATA to pointer of your type
        Timespec *p_type = static_cast<Timespec *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

        // Object that deserializes the data.
        BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

        // Deserialize encapsulation.
        deser.DeserializeEncapsulation();
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Deserialize the object.
        // deser.Deserialize(*p_type);

        return deser.Deserialize(*p_type) == 0;
    }

    std::function<uint32_t()> TimespecPubSubType::GetSerializedSizeProvider(
        void* data)
    {
        return [data]() -> uint32_t
        {
            BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
            size_t current_alignment{0};
            return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                       *static_cast<Timespec *>(data), current_alignment)) +
                   4u /*encapsulation*/;
        };
    }

    void* TimespecPubSubType::CreateData()
    {
        return reinterpret_cast<void*>(new Timespec());
    }

    void TimespecPubSubType::DeleteData(
        void* data)
    {
        delete(reinterpret_cast<Timespec *>(data));
    }

    bool TimespecPubSubType::GetKey(
        void* data,
        InstanceHandle* handle,
        bool force_md5)
    {
        if (!mIsGetKeyDefined)
        {
            return false;
        }

        Timespec* p_type = static_cast<Timespec*>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Timespec_max_key_cdr_typesize);

        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
        SerializeKey(ser, *p_type);
        // if (force_md5 || Timespec_max_key_cdr_typesize > 16)
        // {
        //     m_md5.init();
        //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
        //     m_md5.finalize();
        //     for (uint8_t i = 0; i < 16; ++i)
        //     {
        //         handle->mValue[i] = m_md5.digest[i];
        //     }
        // }
        // else
        {
            for (uint8_t i = 0; i < 16; ++i)
            {
                handle->mValue[i] = mKeyBuffer[i];
            }
        }
        return true;
    }

    NamedValuePubSubType::NamedValuePubSubType()
    {
        SetName("DSF::NamedValue");
        uint32_t type_size = DSF_NamedValue_max_cdr_typesize;

        type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
        mTypeSize = type_size + 4;

        mIsGetKeyDefined = false;

        uint32_t keyLength = DSF_NamedValue_max_key_cdr_typesize > 16 ? DSF_NamedValue_max_key_cdr_typesize : 16;
        mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
        memset(mKeyBuffer, 0, keyLength);
    }

    NamedValuePubSubType::~NamedValuePubSubType()
    {
        if (mKeyBuffer != nullptr)
        {
            free(mKeyBuffer);
        }
    }

    bool NamedValuePubSubType::Serialize(
        void *data,
        SerializedPayload *payload)
    {
        NamedValue *p_type = static_cast<NamedValue *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Serialize encapsulation
        ser.SerializeEncapsulation();
        // Serialize the object.
        auto retcode = ser.Serialize(*p_type);

        // Get the serialized length
        payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
        return retcode == 0;
    }

    bool NamedValuePubSubType::Deserialize(
        SerializedPayload* payload,
        void* data)
    {
        // Convert DATA to pointer of your type
        NamedValue *p_type = static_cast<NamedValue *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

        // Object that deserializes the data.
        BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

        // Deserialize encapsulation.
        deser.DeserializeEncapsulation();
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Deserialize the object.
        // deser.Deserialize(*p_type);

        return deser.Deserialize(*p_type) == 0;
    }

    std::function<uint32_t()> NamedValuePubSubType::GetSerializedSizeProvider(
        void* data)
    {
        return [data]() -> uint32_t
        {
            BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
            size_t current_alignment{0};
            return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                       *static_cast<NamedValue *>(data), current_alignment)) +
                   4u /*encapsulation*/;
        };
    }

    void* NamedValuePubSubType::CreateData()
    {
        return reinterpret_cast<void*>(new NamedValue());
    }

    void NamedValuePubSubType::DeleteData(
        void* data)
    {
        delete(reinterpret_cast<NamedValue *>(data));
    }

    bool NamedValuePubSubType::GetKey(
        void* data,
        InstanceHandle* handle,
        bool force_md5)
    {
        if (!mIsGetKeyDefined)
        {
            return false;
        }

        NamedValue* p_type = static_cast<NamedValue*>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_NamedValue_max_key_cdr_typesize);

        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
        SerializeKey(ser, *p_type);
        // if (force_md5 || NamedValue_max_key_cdr_typesize > 16)
        // {
        //     m_md5.init();
        //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
        //     m_md5.finalize();
        //     for (uint8_t i = 0; i < 16; ++i)
        //     {
        //         handle->mValue[i] = m_md5.digest[i];
        //     }
        // }
        // else
        {
            for (uint8_t i = 0; i < 16; ++i)
            {
                handle->mValue[i] = mKeyBuffer[i];
            }
        }
        return true;
    }

    StructuredDataMapEntryPubSubType::StructuredDataMapEntryPubSubType()
    {
        SetName("DSF::StructuredDataMapEntry");
        uint32_t type_size = DSF_StructuredDataMapEntry_max_cdr_typesize;

        type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
        mTypeSize = type_size + 4;

        mIsGetKeyDefined = false;

        uint32_t keyLength = DSF_StructuredDataMapEntry_max_key_cdr_typesize > 16 ? DSF_StructuredDataMapEntry_max_key_cdr_typesize : 16;
        mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
        memset(mKeyBuffer, 0, keyLength);
    }

    StructuredDataMapEntryPubSubType::~StructuredDataMapEntryPubSubType()
    {
        if (mKeyBuffer != nullptr)
        {
            free(mKeyBuffer);
        }
    }

    bool StructuredDataMapEntryPubSubType::Serialize(
        void *data,
        SerializedPayload *payload)
    {
        StructuredDataMapEntry *p_type = static_cast<StructuredDataMapEntry *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Serialize encapsulation
        ser.SerializeEncapsulation();
        // Serialize the object.
        auto retcode = ser.Serialize(*p_type);

        // Get the serialized length
        payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
        return retcode == 0;
    }

    bool StructuredDataMapEntryPubSubType::Deserialize(
        SerializedPayload* payload,
        void* data)
    {
        // Convert DATA to pointer of your type
        StructuredDataMapEntry *p_type = static_cast<StructuredDataMapEntry *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

        // Object that deserializes the data.
        BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

        // Deserialize encapsulation.
        deser.DeserializeEncapsulation();
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Deserialize the object.
        // deser.Deserialize(*p_type);

        return deser.Deserialize(*p_type) == 0;
    }

    std::function<uint32_t()> StructuredDataMapEntryPubSubType::GetSerializedSizeProvider(
        void* data)
    {
        return [data]() -> uint32_t
        {
            BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
            size_t current_alignment{0};
            return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                       *static_cast<StructuredDataMapEntry *>(data), current_alignment)) +
                   4u /*encapsulation*/;
        };
    }

    void* StructuredDataMapEntryPubSubType::CreateData()
    {
        return reinterpret_cast<void*>(new StructuredDataMapEntry());
    }

    void StructuredDataMapEntryPubSubType::DeleteData(
        void* data)
    {
        delete(reinterpret_cast<StructuredDataMapEntry *>(data));
    }

    bool StructuredDataMapEntryPubSubType::GetKey(
        void* data,
        InstanceHandle* handle,
        bool force_md5)
    {
        if (!mIsGetKeyDefined)
        {
            return false;
        }

        StructuredDataMapEntry* p_type = static_cast<StructuredDataMapEntry*>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_StructuredDataMapEntry_max_key_cdr_typesize);

        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
        SerializeKey(ser, *p_type);
        // if (force_md5 || StructuredDataMapEntry_max_key_cdr_typesize > 16)
        // {
        //     m_md5.init();
        //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
        //     m_md5.finalize();
        //     for (uint8_t i = 0; i < 16; ++i)
        //     {
        //         handle->mValue[i] = m_md5.digest[i];
        //     }
        // }
        // else
        {
            for (uint8_t i = 0; i < 16; ++i)
            {
                handle->mValue[i] = mKeyBuffer[i];
            }
        }
        return true;
    }

    DataBufferPubSubType::DataBufferPubSubType()
    {
        SetName("DSF::DataBuffer");
        uint32_t type_size = DSF_DataBuffer_max_cdr_typesize;

        type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
        mTypeSize = type_size + 4;

        mIsGetKeyDefined = false;

        uint32_t keyLength = DSF_DataBuffer_max_key_cdr_typesize > 16 ? DSF_DataBuffer_max_key_cdr_typesize : 16;
        mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
        memset(mKeyBuffer, 0, keyLength);
    }

    DataBufferPubSubType::~DataBufferPubSubType()
    {
        if (mKeyBuffer != nullptr)
        {
            free(mKeyBuffer);
        }
    }

    bool DataBufferPubSubType::Serialize(
        void *data,
        SerializedPayload *payload)
    {
        DataBuffer *p_type = static_cast<DataBuffer *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Serialize encapsulation
        ser.SerializeEncapsulation();
        // Serialize the object.
        auto retcode = ser.Serialize(*p_type);

        // Get the serialized length
        payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
        return retcode == 0;
    }

    bool DataBufferPubSubType::Deserialize(
        SerializedPayload* payload,
        void* data)
    {
        // Convert DATA to pointer of your type
        DataBuffer *p_type = static_cast<DataBuffer *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

        // Object that deserializes the data.
        BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

        // Deserialize encapsulation.
        deser.DeserializeEncapsulation();
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Deserialize the object.
        // deser.Deserialize(*p_type);

        return deser.Deserialize(*p_type) == 0;
    }

    std::function<uint32_t()> DataBufferPubSubType::GetSerializedSizeProvider(
        void* data)
    {
        return [data]() -> uint32_t
        {
            BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
            size_t current_alignment{0};
            return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                       *static_cast<DataBuffer *>(data), current_alignment)) +
                   4u /*encapsulation*/;
        };
    }

    void* DataBufferPubSubType::CreateData()
    {
        return reinterpret_cast<void*>(new DataBuffer());
    }

    void DataBufferPubSubType::DeleteData(
        void* data)
    {
        delete(reinterpret_cast<DataBuffer *>(data));
    }

    bool DataBufferPubSubType::GetKey(
        void* data,
        InstanceHandle* handle,
        bool force_md5)
    {
        if (!mIsGetKeyDefined)
        {
            return false;
        }

        DataBuffer* p_type = static_cast<DataBuffer*>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_DataBuffer_max_key_cdr_typesize);

        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
        SerializeKey(ser, *p_type);
        // if (force_md5 || DataBuffer_max_key_cdr_typesize > 16)
        // {
        //     m_md5.init();
        //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
        //     m_md5.finalize();
        //     for (uint8_t i = 0; i < 16; ++i)
        //     {
        //         handle->mValue[i] = m_md5.digest[i];
        //     }
        // }
        // else
        {
            for (uint8_t i = 0; i < 16; ++i)
            {
                handle->mValue[i] = mKeyBuffer[i];
            }
        }
        return true;
    }

    namespace DataTypes {
        DT_FILEPubSubType::DT_FILEPubSubType()
        {
            SetName("DSF::DataTypes::DT_FILE");
            uint32_t type_size = DSF_DataTypes_DT_FILE_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_DataTypes_DT_FILE_max_key_cdr_typesize > 16 ? DSF_DataTypes_DT_FILE_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        DT_FILEPubSubType::~DT_FILEPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool DT_FILEPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            DT_FILE *p_type = static_cast<DT_FILE *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool DT_FILEPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            DT_FILE *p_type = static_cast<DT_FILE *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> DT_FILEPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<DT_FILE *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* DT_FILEPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new DT_FILE());
        }

        void DT_FILEPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<DT_FILE *>(data));
        }

        bool DT_FILEPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            DT_FILE* p_type = static_cast<DT_FILE*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_DataTypes_DT_FILE_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || DT_FILE_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }


    } //End of namespace DataTypes


} //End of namespace DSF

