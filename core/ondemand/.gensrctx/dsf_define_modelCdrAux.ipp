// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file dsf_define_modelCdrAux.ipp
 * This source file contains some declarations of CDR related functions.
 *
 * This file was generated by the tool fastddsgen.
 */

#ifndef _DDS_GENERATED_DSF_MODEL_DSF_DEFINE_MODELCDRAUX_IPP_
#define _DDS_GENERATED_DSF_MODEL_DSF_DEFINE_MODELCDRAUX_IPP_

#include "dsf_define_modelCdrAux.hpp"

#include "txcdr/Cdr.h"
#include "txcdr/SerializeCdr.h"
#include "txcdr/DeserializeCdr.h"
#include "txcdr/CdrSizeCalculator.h"
#include "txcdr/MemberId.h"

template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Model::Define& data,
        size_t& current_alignment)
{
    using namespace DSF::Model;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.name(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.version(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.nodeName(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.description(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.dataSchema(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(5),
                data.defaultValue(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(6),
                data.referenceValue(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(7),
                data.events(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(8),
                data.alarms(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Model::Define& data)
{
    using namespace DSF::Model;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.name());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.version());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.nodeName());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.description());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.dataSchema());

        scdr.Serialize(BaoSky::Cdr::MemberId(5));
        scdr.Serialize(data.defaultValue());

        scdr.Serialize(BaoSky::Cdr::MemberId(6));
        scdr.Serialize(data.referenceValue());

        scdr.Serialize(BaoSky::Cdr::MemberId(7));
        scdr.Serialize(data.events());

        scdr.Serialize(BaoSky::Cdr::MemberId(8));
        scdr.Serialize(data.alarms());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Model::Define& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.name());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.version());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.nodeName());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.description());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.dataSchema());
                                    break;

                                case 5:
                                        dcdr.Deserialize(data.defaultValue());
                                    break;

                                case 6:
                                        dcdr.Deserialize(data.referenceValue());
                                    break;

                                case 7:
                                        dcdr.Deserialize(data.events());
                                    break;

                                case 8:
                                        dcdr.Deserialize(data.alarms());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Model::Define& data)
{
    using namespace DSF::Model;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Model::Undefine& data,
        size_t& current_alignment)
{
    using namespace DSF::Model;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.modelIndex(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.nodeName(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Model::Undefine& data)
{
    using namespace DSF::Model;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.modelIndex());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.nodeName());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Model::Undefine& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.modelIndex());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.nodeName());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Model::Undefine& data)
{
    using namespace DSF::Model;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Model::ModelRequest& data,
        size_t& current_alignment)
{
    using namespace DSF::Model;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};

    calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0), data._d(),
                    current_alignment);

    switch (data._d())
    {
        //1
                case DSF::Model::DEFINE_TYPE::MODEL_DEFINE:
                    calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                                data.modelDefine(), current_alignment);
                    break;

                case DSF::Model::DEFINE_TYPE::MODEL_UNDEFINE:
                    calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                                data.modelUndefine(), current_alignment);
                    break;

        default:
            break;
    }

    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}


template<>
void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr &scdr,
        const DSF::Model::ModelRequest& data)
{
    using namespace DSF::Model;

    scdr.CdrBeginSerializeType();
    scdr.Serialize(data._d());

    switch (data._d())
    {
        //2
                case DSF::Model::DEFINE_TYPE::MODEL_DEFINE:
                    scdr.Serialize(data.modelDefine());
                    break;

                case DSF::Model::DEFINE_TYPE::MODEL_UNDEFINE:
                    scdr.Serialize(data.modelUndefine());
                    break;

        default:
            break;
    }

    scdr.CdrEndSerializeType();
}

template<>
void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr &dcdr,
        DSF::Model::ModelRequest& data)
{
    using namespace DSF::Model;

    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                        BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                        BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
            [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
            {
                bool ret_value = true;

                switch (mid)
                {
                    case 0:
                        dcdr.Deserialize(data._d());
                        break;
                    default:
                        switch (data._d())
                        {
                            //3
                                                        case DSF::Model::DEFINE_TYPE::MODEL_DEFINE:
                                                            dcdr.Deserialize(data.modelDefine());
                                                            break;

                                                        case DSF::Model::DEFINE_TYPE::MODEL_UNDEFINE:
                                                            dcdr.Deserialize(data.modelUndefine());
                                                            break;

                            default:
                                break;
                            }
                        ret_value = false;
                        break;
                }
                return ret_value;
            });
}

template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Model::ModelRequestEnvelope& data,
        size_t& current_alignment)
{
    using namespace DSF::Model;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.request(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Model::ModelRequestEnvelope& data)
{
    using namespace DSF::Model;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.request());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Model::ModelRequestEnvelope& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.request());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Model::ModelRequestEnvelope& data)
{
    using namespace DSF::Model;

    static_cast<void>(scdr);
    static_cast<void>(data);
}





#endif // _DDS_GENERATED_DSF_MODEL_DSF_DEFINE_MODELCDRAUX_IPP_

