// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file dsf_define_messagesCdrAux.ipp
 * This source file contains some declarations of CDR related functions.
 *
 * This file was generated by the tool fastddsgen.
 */

#ifndef _DDS_GENERATED_DSF_MESSAGE_DSF_DEFINE_MESSAGESCDRAUX_IPP_
#define _DDS_GENERATED_DSF_MESSAGE_DSF_DEFINE_MESSAGESCDRAUX_IPP_

#include "dsf_define_messagesCdrAux.hpp"

#include "txcdr/Cdr.h"
#include "txcdr/SerializeCdr.h"
#include "txcdr/DeserializeCdr.h"
#include "txcdr/CdrSizeCalculator.h"
#include "txcdr/MemberId.h"

template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::Event& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.eventCode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.payloadType(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.description(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.category(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.createTime(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(5),
                data.msgType(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::Event& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.eventCode());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.payloadType());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.description());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.category());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.createTime());

        scdr.Serialize(BaoSky::Cdr::MemberId(5));
        scdr.Serialize(data.msgType());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::Event& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.eventCode());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.payloadType());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.description());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.category());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.createTime());
                                    break;

                                case 5:
                                        dcdr.Deserialize(data.msgType());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::Event& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::EventTransfer& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.eventCode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.srcNode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.seqNum(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.sourceTime(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.sendTime(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(5),
                data.payloadData(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(6),
                data.payloadBlobType(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(7),
                data.nodeInfo(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(8),
                data.relatedVars(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(9),
                data.relatedNodes(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::EventTransfer& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.eventCode());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.srcNode());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.seqNum());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.sourceTime());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.sendTime());

        scdr.Serialize(BaoSky::Cdr::MemberId(5));
        scdr.Serialize(data.payloadData());

        scdr.Serialize(BaoSky::Cdr::MemberId(6));
        scdr.Serialize(data.payloadBlobType());

        scdr.Serialize(BaoSky::Cdr::MemberId(7));
        scdr.Serialize(data.nodeInfo());

        scdr.Serialize(BaoSky::Cdr::MemberId(8));
        scdr.Serialize(data.relatedVars());

        scdr.Serialize(BaoSky::Cdr::MemberId(9));
        scdr.Serialize(data.relatedNodes());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::EventTransfer& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.eventCode());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.srcNode());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.seqNum());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.sourceTime());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.sendTime());
                                    break;

                                case 5:
                                        dcdr.Deserialize(data.payloadData());
                                    break;

                                case 6:
                                        dcdr.Deserialize(data.payloadBlobType());
                                    break;

                                case 7:
                                        dcdr.Deserialize(data.nodeInfo());
                                    break;

                                case 8:
                                        dcdr.Deserialize(data.relatedVars());
                                    break;

                                case 9:
                                        dcdr.Deserialize(data.relatedNodes());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::EventTransfer& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::NamedBlob& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.name(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.blob(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::NamedBlob& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.name());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.blob());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::NamedBlob& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.name());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.blob());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::NamedBlob& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::ControlVar& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.nodeName(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.type(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.namedValue(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::ControlVar& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.nodeName());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.type());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.namedValue());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::ControlVar& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.nodeName());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.type());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.namedValue());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::ControlVar& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::Control& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.messageId(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.currentTime(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.msgType(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.vars(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.srcNode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(5),
                data.write_tokens(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(6),
                data.user(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::Control& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.messageId());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.currentTime());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.msgType());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.vars());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.srcNode());

        scdr.Serialize(BaoSky::Cdr::MemberId(5));
        scdr.Serialize(data.write_tokens());

        scdr.Serialize(BaoSky::Cdr::MemberId(6));
        scdr.Serialize(data.user());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::Control& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.messageId());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.currentTime());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.msgType());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.vars());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.srcNode());
                                    break;

                                case 5:
                                        dcdr.Deserialize(data.write_tokens());
                                    break;

                                case 6:
                                        dcdr.Deserialize(data.user());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::Control& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::SubTableRegister& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.msgType(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.varFreqs(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.tableName(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.nodeName(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.user(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::SubTableRegister& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.msgType());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.varFreqs());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.tableName());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.nodeName());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.user());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::SubTableRegister& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.msgType());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.varFreqs());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.tableName());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.nodeName());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.user());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::SubTableRegister& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::SubTableUnregister& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.msgType(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.varNames(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.tableName(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.nodeName(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.user(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::SubTableUnregister& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.msgType());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.varNames());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.tableName());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.nodeName());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.user());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::SubTableUnregister& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.msgType());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.varNames());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.tableName());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.nodeName());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.user());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::SubTableUnregister& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::Alarm& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.eventCode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.payloadType(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.description(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.category(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.createTime(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(5),
                data.msgType(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(6),
                data.priority(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(7),
                data.status(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(8),
                data.tmConfirm(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(9),
                data.confirmPerson(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(10),
                data.tmRecovery(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(11),
                data.recoveryNamedValue(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::Alarm& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.eventCode());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.payloadType());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.description());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.category());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.createTime());

        scdr.Serialize(BaoSky::Cdr::MemberId(5));
        scdr.Serialize(data.msgType());

        scdr.Serialize(BaoSky::Cdr::MemberId(6));
        scdr.Serialize(data.priority());

        scdr.Serialize(BaoSky::Cdr::MemberId(7));
        scdr.Serialize(data.status());

        scdr.Serialize(BaoSky::Cdr::MemberId(8));
        scdr.Serialize(data.tmConfirm());

        scdr.Serialize(BaoSky::Cdr::MemberId(9));
        scdr.Serialize(data.confirmPerson());

        scdr.Serialize(BaoSky::Cdr::MemberId(10));
        scdr.Serialize(data.tmRecovery());

        scdr.Serialize(BaoSky::Cdr::MemberId(11));
        scdr.Serialize(data.recoveryNamedValue());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::Alarm& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.eventCode());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.payloadType());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.description());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.category());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.createTime());
                                    break;

                                case 5:
                                        dcdr.Deserialize(data.msgType());
                                    break;

                                case 6:
                                        dcdr.Deserialize(data.priority());
                                    break;

                                case 7:
                                        dcdr.Deserialize(data.status());
                                    break;

                                case 8:
                                        dcdr.Deserialize(data.tmConfirm());
                                    break;

                                case 9:
                                        dcdr.Deserialize(data.confirmPerson());
                                    break;

                                case 10:
                                        dcdr.Deserialize(data.tmRecovery());
                                    break;

                                case 11:
                                        dcdr.Deserialize(data.recoveryNamedValue());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::Alarm& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::NamedRetcode& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.nodeName(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.name(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.retcode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.nativeRetcode(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::NamedRetcode& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.nodeName());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.name());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.retcode());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.nativeRetcode());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::NamedRetcode& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.nodeName());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.name());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.retcode());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.nativeRetcode());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::NamedRetcode& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::CommandRequest& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};

    calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0), data._d(),
                    current_alignment);

    switch (data._d())
    {
        //1
                case DSF::Message::MSGTYPE::CONTROL:
                    calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                                data.controlMsg(), current_alignment);
                    break;

                case DSF::Message::MSGTYPE::EVENT:
                    calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                                data.eventMsg(), current_alignment);
                    break;

                case DSF::Message::MSGTYPE::ALARM:
                    calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                                data.alarmMsg(), current_alignment);
                    break;

        default:
            break;
    }

    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}


template<>
void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr &scdr,
        const DSF::Message::CommandRequest& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();
    scdr.Serialize(data._d());

    switch (data._d())
    {
        //2
                case DSF::Message::MSGTYPE::CONTROL:
                    scdr.Serialize(data.controlMsg());
                    break;

                case DSF::Message::MSGTYPE::EVENT:
                    scdr.Serialize(data.eventMsg());
                    break;

                case DSF::Message::MSGTYPE::ALARM:
                    scdr.Serialize(data.alarmMsg());
                    break;

        default:
            break;
    }

    scdr.CdrEndSerializeType();
}

template<>
void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr &dcdr,
        DSF::Message::CommandRequest& data)
{
    using namespace DSF::Message;

    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                        BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                        BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
            [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
            {
                bool ret_value = true;

                switch (mid)
                {
                    case 0:
                        dcdr.Deserialize(data._d());
                        break;
                    default:
                        switch (data._d())
                        {
                            //3
                                                        case DSF::Message::MSGTYPE::CONTROL:
                                                            dcdr.Deserialize(data.controlMsg());
                                                            break;

                                                        case DSF::Message::MSGTYPE::EVENT:
                                                            dcdr.Deserialize(data.eventMsg());
                                                            break;

                                                        case DSF::Message::MSGTYPE::ALARM:
                                                            dcdr.Deserialize(data.alarmMsg());
                                                            break;

                            default:
                                break;
                            }
                        ret_value = false;
                        break;
                }
                return ret_value;
            });
}

template<>
eProsima_user_DllExport size_t CalculateSerializedSizeUserDefined(
        BaoSky::Cdr::CdrSizeCalculator& calculator,
        const DSF::Message::CommandResponse& data,
        size_t& current_alignment)
{
    using namespace DSF::Message;

    static_cast<void>(data);

    BaoSky::Cdr::EncodingAlgorithmFlag previous_encoding = calculator.GetEncoding();
    size_t calculated_size {calculator.BeginCalculateTypeSerializedSize(
                                BaoSky::Cdr::CdrVersion::XCDRv2 == calculator.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(0),
                data.messageId(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(1),
                data.nodeInfo(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(2),
                data.retCode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(3),
                data.nativeRetcode(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(4),
                data.statusVMessage(), current_alignment);

        calculated_size += calculator.CalculateMemberSerializedSize(BaoSky::Cdr::MemberId(5),
                data.controlRet(), current_alignment);


    calculated_size += calculator.EndCalculateTypeSerializedSize(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void SerializeUserDefined(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::CommandResponse& data)
{
    using namespace DSF::Message;

    scdr.CdrBeginSerializeType();

        scdr.Serialize(BaoSky::Cdr::MemberId(0));
        scdr.Serialize(data.messageId());

        scdr.Serialize(BaoSky::Cdr::MemberId(1));
        scdr.Serialize(data.nodeInfo());

        scdr.Serialize(BaoSky::Cdr::MemberId(2));
        scdr.Serialize(data.retCode());

        scdr.Serialize(BaoSky::Cdr::MemberId(3));
        scdr.Serialize(data.nativeRetcode());

        scdr.Serialize(BaoSky::Cdr::MemberId(4));
        scdr.Serialize(data.statusVMessage());

        scdr.Serialize(BaoSky::Cdr::MemberId(5));
        scdr.Serialize(data.controlRet());

    scdr.CdrEndSerializeType();
}

template<>
eProsima_user_DllExport void DeserializeUserDefined(
        BaoSky::Cdr::DeserializeCdr& dcdr,
        DSF::Message::CommandResponse& data)
{
    dcdr.DeserializeType(BaoSky::Cdr::CdrVersion::XCDRv2 == dcdr.GetCdrVersion() ?
                                BaoSky::Cdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                BaoSky::Cdr::EncodingAlgorithmFlag::PLAIN_CDR,
        [&data](BaoSky::Cdr::DeserializeCdr &dcdr, const uint32_t &mid) -> bool
        {
            bool ret_value = true;
            switch (mid)
            {
                                case 0:
                                        dcdr.Deserialize(data.messageId());
                                    break;

                                case 1:
                                        dcdr.Deserialize(data.nodeInfo());
                                    break;

                                case 2:
                                        dcdr.Deserialize(data.retCode());
                                    break;

                                case 3:
                                        dcdr.Deserialize(data.nativeRetcode());
                                    break;

                                case 4:
                                        dcdr.Deserialize(data.statusVMessage());
                                    break;

                                case 5:
                                        dcdr.Deserialize(data.controlRet());
                                    break;

                default:
                    ret_value = false;
                    break;
            }
            return ret_value;
        });

}

void serialize_key(
        BaoSky::Cdr::SerializeCdr& scdr,
        const DSF::Message::CommandResponse& data)
{
    using namespace DSF::Message;

    static_cast<void>(scdr);
    static_cast<void>(data);
}





#endif // _DDS_GENERATED_DSF_MESSAGE_DSF_DEFINE_MESSAGESCDRAUX_IPP_

