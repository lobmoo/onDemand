// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file dsf_define_nodePubSubTypes.cpp
 * This header file contains the implementation of the serialization functions.
 *
 * This file was generated by the tool fastddsgen.
 */

#include "txdds/RTPS/common/SerializedPayload.h"

#include "txcdr/Cdr.h"
#include "txcdr/CdrSizeCalculator.h"
#include "txcdr/SerializeCdr.h"
#include "txcdr/DeserializeCdr.h"

#include "dsf_define_nodePubSubTypes.h"
#include "dsf_define_nodeCdrAux.hpp"

using SerializedPayload = BaoSky::rtps::SerializedPayload;
using InstanceHandle = BaoSky::rtps::InstanceHandle;

namespace DSF {
    NetworkInterfacePubSubType::NetworkInterfacePubSubType()
    {
        SetName("DSF::NetworkInterface");
        uint32_t type_size = DSF_NetworkInterface_max_cdr_typesize;

        type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
        mTypeSize = type_size + 4;

        mIsGetKeyDefined = false;

        uint32_t keyLength = DSF_NetworkInterface_max_key_cdr_typesize > 16 ? DSF_NetworkInterface_max_key_cdr_typesize : 16;
        mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
        memset(mKeyBuffer, 0, keyLength);
    }

    NetworkInterfacePubSubType::~NetworkInterfacePubSubType()
    {
        if (mKeyBuffer != nullptr)
        {
            free(mKeyBuffer);
        }
    }

    bool NetworkInterfacePubSubType::Serialize(
        void *data,
        SerializedPayload *payload)
    {
        NetworkInterface *p_type = static_cast<NetworkInterface *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Serialize encapsulation
        ser.SerializeEncapsulation();
        // Serialize the object.
        auto retcode = ser.Serialize(*p_type);

        // Get the serialized length
        payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
        return retcode == 0;
    }

    bool NetworkInterfacePubSubType::Deserialize(
        SerializedPayload* payload,
        void* data)
    {
        // Convert DATA to pointer of your type
        NetworkInterface *p_type = static_cast<NetworkInterface *>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

        // Object that deserializes the data.
        BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

        // Deserialize encapsulation.
        deser.DeserializeEncapsulation();
        payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        // Deserialize the object.
        // deser.Deserialize(*p_type);

        return deser.Deserialize(*p_type) == 0;
    }

    std::function<uint32_t()> NetworkInterfacePubSubType::GetSerializedSizeProvider(
        void* data)
    {
        return [data]() -> uint32_t
        {
            BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
            size_t current_alignment{0};
            return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                       *static_cast<NetworkInterface *>(data), current_alignment)) +
                   4u /*encapsulation*/;
        };
    }

    void* NetworkInterfacePubSubType::CreateData()
    {
        return reinterpret_cast<void*>(new NetworkInterface());
    }

    void NetworkInterfacePubSubType::DeleteData(
        void* data)
    {
        delete(reinterpret_cast<NetworkInterface *>(data));
    }

    bool NetworkInterfacePubSubType::GetKey(
        void* data,
        InstanceHandle* handle,
        bool force_md5)
    {
        if (!mIsGetKeyDefined)
        {
            return false;
        }

        NetworkInterface* p_type = static_cast<NetworkInterface*>(data);

        // Object that manages the raw buffer.
        BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_NetworkInterface_max_key_cdr_typesize);

        // Object that serializes the data.
        BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
        SerializeKey(ser, *p_type);
        // if (force_md5 || NetworkInterface_max_key_cdr_typesize > 16)
        // {
        //     m_md5.init();
        //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
        //     m_md5.finalize();
        //     for (uint8_t i = 0; i < 16; ++i)
        //     {
        //         handle->mValue[i] = m_md5.digest[i];
        //     }
        // }
        // else
        {
            for (uint8_t i = 0; i < 16; ++i)
            {
                handle->mValue[i] = mKeyBuffer[i];
            }
        }
        return true;
    }

    namespace Node {
        BasicInfoPubSubType::BasicInfoPubSubType()
        {
            SetName("DSF::Node::BasicInfo");
            uint32_t type_size = DSF_Node_BasicInfo_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Node_BasicInfo_max_key_cdr_typesize > 16 ? DSF_Node_BasicInfo_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        BasicInfoPubSubType::~BasicInfoPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool BasicInfoPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            BasicInfo *p_type = static_cast<BasicInfo *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool BasicInfoPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            BasicInfo *p_type = static_cast<BasicInfo *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> BasicInfoPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<BasicInfo *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* BasicInfoPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new BasicInfo());
        }

        void BasicInfoPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<BasicInfo *>(data));
        }

        bool BasicInfoPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            BasicInfo* p_type = static_cast<BasicInfo*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Node_BasicInfo_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || BasicInfo_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        RuntimeInfoPubSubType::RuntimeInfoPubSubType()
        {
            SetName("DSF::Node::RuntimeInfo");
            uint32_t type_size = DSF_Node_RuntimeInfo_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Node_RuntimeInfo_max_key_cdr_typesize > 16 ? DSF_Node_RuntimeInfo_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        RuntimeInfoPubSubType::~RuntimeInfoPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool RuntimeInfoPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            RuntimeInfo *p_type = static_cast<RuntimeInfo *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool RuntimeInfoPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            RuntimeInfo *p_type = static_cast<RuntimeInfo *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> RuntimeInfoPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<RuntimeInfo *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* RuntimeInfoPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new RuntimeInfo());
        }

        void RuntimeInfoPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<RuntimeInfo *>(data));
        }

        bool RuntimeInfoPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            RuntimeInfo* p_type = static_cast<RuntimeInfo*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Node_RuntimeInfo_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || RuntimeInfo_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }


    } //End of namespace Node


} //End of namespace DSF

