// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file dsf_define_varPubSubTypes.cpp
 * This header file contains the implementation of the serialization functions.
 *
 * This file was generated by the tool fastddsgen.
 */

#include "txdds/RTPS/common/SerializedPayload.h"

#include "txcdr/Cdr.h"
#include "txcdr/CdrSizeCalculator.h"
#include "txcdr/SerializeCdr.h"
#include "txcdr/DeserializeCdr.h"

#include "dsf_define_varPubSubTypes.h"
#include "dsf_define_varCdrAux.hpp"

using SerializedPayload = BaoSky::rtps::SerializedPayload;
using InstanceHandle = BaoSky::rtps::InstanceHandle;

namespace DSF {
    namespace Var {
        AccessControlPubSubType::AccessControlPubSubType()
        {
            SetName("DSF::Var::AccessControl");
            uint32_t type_size = DSF_Var_AccessControl_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_AccessControl_max_key_cdr_typesize > 16 ? DSF_Var_AccessControl_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        AccessControlPubSubType::~AccessControlPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool AccessControlPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            AccessControl *p_type = static_cast<AccessControl *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool AccessControlPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            AccessControl *p_type = static_cast<AccessControl *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> AccessControlPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<AccessControl *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* AccessControlPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new AccessControl());
        }

        void AccessControlPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<AccessControl *>(data));
        }

        bool AccessControlPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            AccessControl* p_type = static_cast<AccessControl*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_AccessControl_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || AccessControl_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        DefinePubSubType::DefinePubSubType()
        {
            SetName("DSF::Var::Define");
            uint32_t type_size = DSF_Var_Define_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_Define_max_key_cdr_typesize > 16 ? DSF_Var_Define_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        DefinePubSubType::~DefinePubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool DefinePubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            Define *p_type = static_cast<Define *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool DefinePubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            Define *p_type = static_cast<Define *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> DefinePubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<Define *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* DefinePubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new Define());
        }

        void DefinePubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<Define *>(data));
        }

        bool DefinePubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            Define* p_type = static_cast<Define*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_Define_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || Define_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        UndefinePubSubType::UndefinePubSubType()
        {
            SetName("DSF::Var::Undefine");
            uint32_t type_size = DSF_Var_Undefine_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_Undefine_max_key_cdr_typesize > 16 ? DSF_Var_Undefine_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        UndefinePubSubType::~UndefinePubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool UndefinePubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            Undefine *p_type = static_cast<Undefine *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool UndefinePubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            Undefine *p_type = static_cast<Undefine *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> UndefinePubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<Undefine *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* UndefinePubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new Undefine());
        }

        void UndefinePubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<Undefine *>(data));
        }

        bool UndefinePubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            Undefine* p_type = static_cast<Undefine*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_Undefine_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || Undefine_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        VarRequestEnvelopePubSubType::VarRequestEnvelopePubSubType()
        {
            SetName("DSF::Var::VarRequestEnvelope");
            uint32_t type_size = DSF_Var_VarRequestEnvelope_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_VarRequestEnvelope_max_key_cdr_typesize > 16 ? DSF_Var_VarRequestEnvelope_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        VarRequestEnvelopePubSubType::~VarRequestEnvelopePubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool VarRequestEnvelopePubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            VarRequestEnvelope *p_type = static_cast<VarRequestEnvelope *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool VarRequestEnvelopePubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            VarRequestEnvelope *p_type = static_cast<VarRequestEnvelope *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> VarRequestEnvelopePubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<VarRequestEnvelope *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* VarRequestEnvelopePubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new VarRequestEnvelope());
        }

        void VarRequestEnvelopePubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<VarRequestEnvelope *>(data));
        }

        bool VarRequestEnvelopePubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            VarRequestEnvelope* p_type = static_cast<VarRequestEnvelope*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_VarRequestEnvelope_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || VarRequestEnvelope_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        DataTransferPubSubType::DataTransferPubSubType()
        {
            SetName("DSF::Var::DataTransfer");
            uint32_t type_size = DSF_Var_DataTransfer_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_DataTransfer_max_key_cdr_typesize > 16 ? DSF_Var_DataTransfer_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        DataTransferPubSubType::~DataTransferPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool DataTransferPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            DataTransfer *p_type = static_cast<DataTransfer *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool DataTransferPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            DataTransfer *p_type = static_cast<DataTransfer *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> DataTransferPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<DataTransfer *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* DataTransferPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new DataTransfer());
        }

        void DataTransferPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<DataTransfer *>(data));
        }

        bool DataTransferPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            DataTransfer* p_type = static_cast<DataTransfer*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_DataTransfer_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || DataTransfer_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        DataTransferExPubSubType::DataTransferExPubSubType()
        {
            SetName("DSF::Var::DataTransferEx");
            uint32_t type_size = DSF_Var_DataTransferEx_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_DataTransferEx_max_key_cdr_typesize > 16 ? DSF_Var_DataTransferEx_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        DataTransferExPubSubType::~DataTransferExPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool DataTransferExPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            DataTransferEx *p_type = static_cast<DataTransferEx *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool DataTransferExPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            DataTransferEx *p_type = static_cast<DataTransferEx *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> DataTransferExPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<DataTransferEx *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* DataTransferExPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new DataTransferEx());
        }

        void DataTransferExPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<DataTransferEx *>(data));
        }

        bool DataTransferExPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            DataTransferEx* p_type = static_cast<DataTransferEx*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_DataTransferEx_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || DataTransferEx_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        VarPubSubInfoPubSubType::VarPubSubInfoPubSubType()
        {
            SetName("DSF::Var::VarPubSubInfo");
            uint32_t type_size = DSF_Var_VarPubSubInfo_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_VarPubSubInfo_max_key_cdr_typesize > 16 ? DSF_Var_VarPubSubInfo_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        VarPubSubInfoPubSubType::~VarPubSubInfoPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool VarPubSubInfoPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            VarPubSubInfo *p_type = static_cast<VarPubSubInfo *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool VarPubSubInfoPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            VarPubSubInfo *p_type = static_cast<VarPubSubInfo *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> VarPubSubInfoPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<VarPubSubInfo *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* VarPubSubInfoPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new VarPubSubInfo());
        }

        void VarPubSubInfoPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<VarPubSubInfo *>(data));
        }

        bool VarPubSubInfoPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            VarPubSubInfo* p_type = static_cast<VarPubSubInfo*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_VarPubSubInfo_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || VarPubSubInfo_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        PubTableVarDefinePubSubType::PubTableVarDefinePubSubType()
        {
            SetName("DSF::Var::PubTableVarDefine");
            uint32_t type_size = DSF_Var_PubTableVarDefine_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_PubTableVarDefine_max_key_cdr_typesize > 16 ? DSF_Var_PubTableVarDefine_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        PubTableVarDefinePubSubType::~PubTableVarDefinePubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool PubTableVarDefinePubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            PubTableVarDefine *p_type = static_cast<PubTableVarDefine *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool PubTableVarDefinePubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            PubTableVarDefine *p_type = static_cast<PubTableVarDefine *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> PubTableVarDefinePubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<PubTableVarDefine *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* PubTableVarDefinePubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new PubTableVarDefine());
        }

        void PubTableVarDefinePubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<PubTableVarDefine *>(data));
        }

        bool PubTableVarDefinePubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            PubTableVarDefine* p_type = static_cast<PubTableVarDefine*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_PubTableVarDefine_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || PubTableVarDefine_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        PubTableDefinePubSubType::PubTableDefinePubSubType()
        {
            SetName("DSF::Var::PubTableDefine");
            uint32_t type_size = DSF_Var_PubTableDefine_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_PubTableDefine_max_key_cdr_typesize > 16 ? DSF_Var_PubTableDefine_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        PubTableDefinePubSubType::~PubTableDefinePubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool PubTableDefinePubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            PubTableDefine *p_type = static_cast<PubTableDefine *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool PubTableDefinePubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            PubTableDefine *p_type = static_cast<PubTableDefine *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> PubTableDefinePubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<PubTableDefine *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* PubTableDefinePubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new PubTableDefine());
        }

        void PubTableDefinePubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<PubTableDefine *>(data));
        }

        bool PubTableDefinePubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            PubTableDefine* p_type = static_cast<PubTableDefine*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_PubTableDefine_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || PubTableDefine_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        TableDataTransferPubSubType::TableDataTransferPubSubType()
        {
            SetName("DSF::Var::TableDataTransfer");
            uint32_t type_size = DSF_Var_TableDataTransfer_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_TableDataTransfer_max_key_cdr_typesize > 16 ? DSF_Var_TableDataTransfer_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        TableDataTransferPubSubType::~TableDataTransferPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool TableDataTransferPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            TableDataTransfer *p_type = static_cast<TableDataTransfer *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool TableDataTransferPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            TableDataTransfer *p_type = static_cast<TableDataTransfer *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> TableDataTransferPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<TableDataTransfer *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* TableDataTransferPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new TableDataTransfer());
        }

        void TableDataTransferPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<TableDataTransfer *>(data));
        }

        bool TableDataTransferPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            TableDataTransfer* p_type = static_cast<TableDataTransfer*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_TableDataTransfer_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || TableDataTransfer_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        TableUpdateBatchPubSubType::TableUpdateBatchPubSubType()
        {
            SetName("DSF::Var::TableUpdateBatch");
            uint32_t type_size = DSF_Var_TableUpdateBatch_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_TableUpdateBatch_max_key_cdr_typesize > 16 ? DSF_Var_TableUpdateBatch_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        TableUpdateBatchPubSubType::~TableUpdateBatchPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool TableUpdateBatchPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            TableUpdateBatch *p_type = static_cast<TableUpdateBatch *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool TableUpdateBatchPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            TableUpdateBatch *p_type = static_cast<TableUpdateBatch *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> TableUpdateBatchPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<TableUpdateBatch *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* TableUpdateBatchPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new TableUpdateBatch());
        }

        void TableUpdateBatchPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<TableUpdateBatch *>(data));
        }

        bool TableUpdateBatchPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            TableUpdateBatch* p_type = static_cast<TableUpdateBatch*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_TableUpdateBatch_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || TableUpdateBatch_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }

        TableDataTransferExPubSubType::TableDataTransferExPubSubType()
        {
            SetName("DSF::Var::TableDataTransferEx");
            uint32_t type_size = DSF_Var_TableDataTransferEx_max_cdr_typesize;

            type_size += static_cast<uint32_t>(BaoSky::Cdr::Cdr::Alignment(type_size, 4)); /* possible submessage alignment */
            mTypeSize = type_size + 4;

            mIsGetKeyDefined = false;

            uint32_t keyLength = DSF_Var_TableDataTransferEx_max_key_cdr_typesize > 16 ? DSF_Var_TableDataTransferEx_max_key_cdr_typesize : 16;
            mKeyBuffer = reinterpret_cast<unsigned char*>(malloc(keyLength));
            memset(mKeyBuffer, 0, keyLength);
        }

        TableDataTransferExPubSubType::~TableDataTransferExPubSubType()
        {
            if (mKeyBuffer != nullptr)
            {
                free(mKeyBuffer);
            }
        }

        bool TableDataTransferExPubSubType::Serialize(
            void *data,
            SerializedPayload *payload)
        {
            TableDataTransferEx *p_type = static_cast<TableDataTransferEx *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->max_size);
            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Serialize encapsulation
            ser.SerializeEncapsulation();
            // Serialize the object.
            auto retcode = ser.Serialize(*p_type);

            // Get the serialized length
            payload->length = static_cast<uint32_t>(ser.GetSerializedDataLength());
            return retcode == 0;
        }

        bool TableDataTransferExPubSubType::Deserialize(
            SerializedPayload* payload,
            void* data)
        {
            // Convert DATA to pointer of your type
            TableDataTransferEx *p_type = static_cast<TableDataTransferEx *>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(payload->data), payload->length);

            // Object that deserializes the data.
            BaoSky::Cdr::DeserializeCdr deser(buffer, BaoSky::Cdr::Cdr::DEFAULT_ENDIAN, BaoSky::Cdr::CdrVersion::XCDRv1);

            // Deserialize encapsulation.
            deser.DeserializeEncapsulation();
            payload->mIdentifier = BaoSky::Cdr::Cdr::DEFAULT_ENDIAN == BaoSky::Cdr::Endianness::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

            // Deserialize the object.
            // deser.Deserialize(*p_type);

            return deser.Deserialize(*p_type) == 0;
        }

        std::function<uint32_t()> TableDataTransferExPubSubType::GetSerializedSizeProvider(
            void* data)
        {
            return [data]() -> uint32_t
            {
                BaoSky::Cdr::CdrSizeCalculator calculator(BaoSky::Cdr::CdrVersion::XCDRv1);
                size_t current_alignment{0};
                return static_cast<uint32_t>(calculator.CalculateSerializedSize(
                           *static_cast<TableDataTransferEx *>(data), current_alignment)) +
                       4u /*encapsulation*/;
            };
        }

        void* TableDataTransferExPubSubType::CreateData()
        {
            return reinterpret_cast<void*>(new TableDataTransferEx());
        }

        void TableDataTransferExPubSubType::DeleteData(
            void* data)
        {
            delete(reinterpret_cast<TableDataTransferEx *>(data));
        }

        bool TableDataTransferExPubSubType::GetKey(
            void* data,
            InstanceHandle* handle,
            bool force_md5)
        {
            if (!mIsGetKeyDefined)
            {
                return false;
            }

            TableDataTransferEx* p_type = static_cast<TableDataTransferEx*>(data);

            // Object that manages the raw buffer.
            BaoSky::Cdr::TXBuffer buffer(reinterpret_cast<char*>(mKeyBuffer), DSF_Var_TableDataTransferEx_max_key_cdr_typesize);

            // Object that serializes the data.
            BaoSky::Cdr::SerializeCdr ser(buffer, BaoSky::Cdr::Endianness::BIG_ENDIANNESS, BaoSky::Cdr::CdrVersion::XCDRv1);
            SerializeKey(ser, *p_type);
            // if (force_md5 || TableDataTransferEx_max_key_cdr_typesize > 16)
            // {
            //     m_md5.init();
            //     m_md5.update(mKeyBuffer, static_cast<unsigned int>(ser.get_serialized_data_length()));
            //     m_md5.finalize();
            //     for (uint8_t i = 0; i < 16; ++i)
            //     {
            //         handle->mValue[i] = m_md5.digest[i];
            //     }
            // }
            // else
            {
                for (uint8_t i = 0; i < 16; ++i)
                {
                    handle->mValue[i] = mKeyBuffer[i];
                }
            }
            return true;
        }


    } //End of namespace Var


} //End of namespace DSF

